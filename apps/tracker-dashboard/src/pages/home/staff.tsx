import { PageLayout } from '@/layouts';
import { UserGreeting } from '@/shared/components';

export const StaffHomePage = () => {
  return (
    <PageLayout>
      <UserGreeting />
    </PageLayout>
  );
};
